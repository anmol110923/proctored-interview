# Optional computer-vision / ML stack for webcam proctoring.
# Needs system packages: ffmpeg, portaudio (macOS: brew install ffmpeg portaudio)
# YOLO weights download on first ObjectDetector run.

opencv-python>=4.5.0
facenet-pytorch>=2.5.0
mediapipe>=0.8.0
ultralytics>=8.0.0
torch>=1.7.0
torchvision>=0.8.0
torchaudio>=2.1.0
pyaudio>=0.2.13
webrtcvad>=2.0.10
